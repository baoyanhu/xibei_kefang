# 2026-08-18 任务清单

> 状态：已完成（用户已确认「修好」后执行）

## 待办

- [x] 1. 诊断 codegraph MCP 未挂载原因：各级配置（`~/.zcode/cli/config.json`、`~/.agents/mcp.json`、工作区 `.zcode/`、`.agents/`、opencode 全局配置）均无 codegraph MCP 注册
- [x] 2. 确认 `.codegraph/daemon.log` 指向 ai1 socket 为历史遗留（ai1 为本项目改名前的旧路径，现已改名为 kefang，daemon 已停止），无需修复
- [x] 3. 验证 `codegraph serve --mcp` 响应 MCP initialize 握手（返回 serverInfo codegraph v1.5.0）
- [x] 4. 新建 `~/.zcode/cli/config.json`，以 stdio 注册 codegraph MCP（command 用绝对路径 `/Users/baoyanhu/.local/bin/codegraph`）
- [x] 5. JSON 语法校验
- [x] 6. 输出完成报告，提醒用户重启会话验证 `mcp__codegraph__*` 工具可用

## 已完成

- [x] codegraph MCP 已注册到 ZCode 用户级配置（对所有项目生效，按 cwd 自动选用各项目自己的索引）

## 测试结果

- 命令：`echo '{"jsonrpc":"2.0","id":1,"method":"initialize",...}' | codegraph serve --mcp`
- 输出：`{"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2024-11-05","capabilities":{"tools":{}},"serverInfo":{"codegraph","version":"1.5.0"},...}}`
- 命令：`python3 -m json.tool ~/.zcode/cli/config.json`
- 输出：解析成功，JSON 校验通过
- 待用户重启会话后验证：`mcp__codegraph__*` 工具出现在会话中

---

# 任务二：基础配置—问卷配置两个接口（详情 + 保存）

> 状态：**待确认**（等用户确认字段清单与定位方式后执行）

## 待办

- [ ] 1. 生成实现方案文档 `docs/requirements/basic-config/questionnaire-config/implementation-plan.md`
- [ ] 2. 加载 php-code-style 技能（写 PHP 前强制）
- [ ] 3. 新增 `QuestionnaireModel`（表名/字段白名单/类型转换/自动时间戳/软删除）
- [ ] 4. 新增 `php/app/service/admin/QuestionnaireService.php`：getDetail + save（校验、active_flag 互斥启用、审计同事务）
- [ ] 5. 新增 `php/app/controller/admin/Questionnaire.php`：detailOp + saveOp（三段式 save_log + returnJson）
- [ ] 6. 注册路由 `POST /admin/questionnaire/detail` + `POST /admin/questionnaire/save`，挂 AdminSign
- [ ] 7. 影响面检查：codegraph 查 questionnaires 表消费方、AuditLogModel 常量扩展、路由无冲突
- [ ] 8. 补全接口文档 `docs/api/customer-survey.html`（新增两个接口区块）
- [ ] 9. 自我审查：三层架构、字段白名单、全 POST、无 Mock、不改 vue/
- [ ] 10. 测试验证：php -l、composer dump-autoload、route:list、真实库 getDetail/save 回滚、非法参数拦截、curl 验签拦截

## 待确认问题（已列在对话中）

1. 详情/保存按 questionnaire_id 还是 merchant_id 定位
2. 字段清单是否与原型一致（原型图模型无法读取，按表结构 + OCR 数字线索反推）
3. save 是否支持新增问卷、是否包含启用状态（status/active_flag）切换
