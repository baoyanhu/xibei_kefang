# 2026-08-19 任务清单

> 状态：已完成

## 任务

B 端「问卷列表」模块全部功能(原型 https://survey-admin.xibeitest.com/questionnaires,已逐项点击勘察完毕)。

## 待办

- [x] 1. 写实现方案文档 `docs/requirements/questionnaire-list/implementation-plan.md`
- [x] 2. 新建 5 个 Model:QuestionnaireModel / QuestionGroupModel / QuestionModel / QuestionOptionModel / NpsScoreImageModel
- [x] 3. 新建 `SurveyService`:list / detail / save / copy / setStatus / delete 六个业务方法
- [x] 4. 新建 `Survey` 控制器(六个 Op 方法,三段式 save_log + try-catch + returnJson)
- [x] 5. `route/app.php` admin 分组追加 6 条 POST 路由 + AdminSign 中间件
- [x] 6. `AuditLogModel` 追加问卷管理相关 action/target 常量
- [x] 7. 影响面检查:确认不动 `admin/questionnaire/*`(基础配置已占用)、不动 vue/、不改动既有表结构
- [x] 8. 自我审查:三层分层 / 构造器注入 / 静态代理 / 全 POST / save_log 三段式 / 无跨端修改
- [x] 9. 测试验证:php -l 全量 + route:list + 真实签名请求(列表/详情/保存/复制/启用互斥/禁用/删除)
- [x] 10. 接口文档补全:`docs/api/customer-survey.html` 追加 B07-B12 六个接口区块
- [x] 11. 输出完成报告,提醒用户测试验证与提交

## 已完成

- [x] 原型勘察:列表/详情/编辑器/7 种题型/跳题/补充说明/分值图标/菜品选择/各确认弹窗全部点击确认
- [x] 表结构核对:questionnaires / question_groups / questions / question_options / nps_score_images / users 与原型功能一一对应,零 DDL
- [x] 6 个接口全部实现并通过 41 项端到端断言(真实 MD5 签名 + AES biz_content 通道)

## 测试结果

- 命令:`php -l`(9 个改动文件)
- 输出:全部 `No syntax errors detected`
- 命令:`php think route:list | grep survey`
- 输出:6 条 `admin/survey/{list,detail,save,copy,status,delete}` 均为 POST 指向 `Survey/*Op`
- 命令:`php tests_survey_crud.php`(真实签名 HTTP 集成测试,跑完已删除)
- 输出:`===== 结果: PASS=41 FAIL=0 =====`
- 覆盖:list 分页/筛选/字段、save 新增+编辑整卷替换、7 题型 config 校验、跳题(对象/数组两种入参形态、thanks、非法目标拦截)、copy 深拷贝+跳题重映射、enable 同商户互斥(旧卷自动禁用+已恢复)、disable、启用中删除拦截、软删后列表/详情不可见、审计 4 类 action 落库
- 中途发现并修复 3 个问题:① 新增缺 trigger_mode 默认值(MySQL 1364)→ 建 Model 常量默认立即推送;② 跳题对象键被 json_decode 转整型导致位置差一 → array_is_list 区分两种形态;③ 非选择题误写 options_extras → 仅单选/多选落
- 命令:MySQL MCP 复核数据残留
- 输出:测试数据已物理清理(leftover=0,问卷 35 保持唯一启用,测试审计已清,库恢复初始状态)
- 命令:门禁 grep(`Log::|error_log|var_dump|::instance()|app()->make`)
- 输出:`GATE_CLEAN`(新增代码零命中)
- 命令:node 校验接口文档 apis 数组
- 输出:`api_count=12`(B01-B12)、`STRUCT_OK`

## 说明

- 测试期间曾短暂启用测试卷使问卷 35 被自动禁用,测试脚本已将其恢复 enabled,并经 MCP 复核
- 临时测试脚本 `php/tests_survey_crud.php` 验证完成后已删除,不入库

---

# 2026-08-19 任务二:评价推送列表(B 端)
> 状态:已完成(集成测试 28/28 通过,零数据污染)

## 待办
- [x] 1. 原型勘察 push-list 页面(筛选 6 项/表格 10 列/分页/无行操作;组件源码拿到状态映射与接口契约)
- [x] 2. 表结构核对:survey_instances / survey_dishes 与原型字段映射,零 DDL
- [x] 3. 写实现方案 `docs/requirements/push-list/implementation-plan.md`,用户确认执行
- [x] 4. SurveyInstanceModel + SurveyDishModel + PushListService + PushList 控制器 + 路由 admin/push/list
- [x] 5. php -l 5 文件全过 + route:list 注册核对 + 集成测试 28/28(纯查询,现有 48 条数据)
- [x] 6. 门禁 grep GATE_CLEAN + 接口文档 B13 补全(api_count=13 STRUCT_OK) + 完成报告

## 测试结果(任务二)
- 命令:`php -l`(5 个改动文件)
- 输出:全部 `No syntax errors detected`
- 命令:`php think route:list | grep push`
- 输出:`admin/push/list → app\controller\admin\PushList/listOp | post`
- 命令:`php tests_push_list.php`(真实 MD5 签名 + AES biz_content 通道,跑完已删除)
- 输出:`===== 结果: PASS=28 FAIL=0 =====`
- 覆盖:无条件分页/倒序、11 字段齐全、order_no/member_card_no/phone 模糊、push_state(pending=1/pushed=47)、answer_state(submitted=12/opened=7/unopened=26)、双状态交集与互斥、单日闭区间边界、dishes 聚合排序、卡号脱敏(888****0001)、3 个非法参数 250、分页末页 8 条
- 中途发现:方案阶段经 MySQL MCP 预核的 4 个预期值有时区/样本偏差(MCP 显示 UTC,实际北京时间;样本前 8 条恰好同卡号),用 PHP PDO 直查修正预期值后全过——**接口行为自始与 SQL 直查一致,代码零返工**
- 数据零污染复核(MCP):instances=48 / dishes=118 / pending=1 / max(update_time) 未变
- 门禁 grep(`Log::|error_log|var_dump|::instance()|app()->make`,4 个新增文件):零命中 GATE_CLEAN

---

# 2026-08-19 任务三:顾客评价(B 端)
> 状态:已完成(集成测试 21/21 通过,零数据污染;**导出经用户确认本期不做**)

## 待办
- [x] 1. 原型勘察 customer-answers 页面(筛选 2 项/8 列/明细抽屉/导出/组件源码)
- [x] 2. 表结构核对:answers 自包含 payload + 关联链,零 DDL
- [x] 3. 实现方案 `docs/requirements/customer-answers/implementation-plan.md`,用户确认(调整:砍掉 B16 导出)
- [x] 4. AnswerModel + CustomerAnswerService(getList/getDetail) + CustomerAnswer 控制器 + 路由×2
- [x] 5. php -l 4 文件全过 + route:list 2 条 + 集成测试 21/21(PDO 动态预期值)
- [x] 6. 门禁 grep GATE_CLEAN + 接口文档 B14/B15(api_count=15 STRUCT_OK) + 完成报告

## 测试结果(任务三)
- 命令:`php -l`(4 个改动文件)/ `php think route:list | grep answer`
- 输出:全部 No syntax errors detected;`admin/answer/{list,detail}` 均为 POST 指向 CustomerAnswer/*Op
- 命令:`php tests_customer_answers.php`(真实 MD5+AES 通道,跑完已删除)
- 输出:`===== 结果: PASS=21 FAIL=0 =====`
- 覆盖:列表 count/倒序/字段/分页、questions 与库内 payload 逐题一致性、问卷名称模糊(命中黄金样本)、单日闭区间、明细卡号脱敏、维度题 config.dimensions 补齐、菜品题 followups 原样透传、dish_name_map 与 survey_dishes 一致、3 个异常分支(缺参/不存在/非法日期 → 250)
- **中途发现并修复**:answers 表存在孤儿答卷(id=40 TC-EXPIRED-TEST,其实例已被历史清理),INNER JOIN 会静默丢行使 count=11≠12 → 改 leftJoin 兜底(问卷列显示 --),符合「payload 自包含快照」语义
- 预期值全部由测试脚本 PDO 直查动态生成(吸取任务二 MCP 时区教训),零手工预估
- 零污染复核(MCP):answers=12 / instances=48 / 无 answer 审计写入 / max(update_time) 未变
