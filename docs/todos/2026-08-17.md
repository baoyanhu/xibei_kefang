# 2026-08-17 任务清单

> 状态：待确认

## 待办

- [ ] 1. 删除 `php/config/database.php` 中非原生的 `socket` 连接配置
- [ ] 2. 删除 `php/.env` 和 `php/.env.example` 中多余的 `socket=` 配置项
- [ ] 3. 影响面检查：确认 PHP 代码内无 `database.socket` / `socket` 数据库配置引用
- [ ] 4. 自我审查：确认仅改动 PHP 端上述 3 个配置文件，不改 Vue
- [ ] 5. 测试验证：检查 INI 可解析，并通过 `127.0.0.1:3306` 执行只读连通性测试

## 已完成

## 测试结果

- 待执行
