# 2026-08-17 任务清单

> 状态：已完成

## 待办

- [x] 1. 删除 `php/config/database.php` 中非原生的 `socket` 连接配置
- [x] 2. 删除 `php/.env` 和 `php/.env.example` 中多余的 `socket=` 配置项
- [x] 3. 影响面检查：确认 PHP 代码内无 `database.socket` / `socket` 数据库配置引用
- [x] 4. 自我审查：确认仅改动 PHP 端上述 3 个配置文件，不改 Vue
- [x] 5. 测试验证：检查 INI 可解析，并通过 `127.0.0.1:3306` 执行只读连通性测试
- [x] 6. 生成基础配置/样式配置实现方案文档
- [x] 7. 实现样式配置详情与保存两个 B 端接口（Controller → Service → Model）
- [x] 8. 影响面检查：路由、`merchant_configs` 配置消费方、审计日志及 Vue 隔离
- [x] 9. 生成项目统一 HTML 接口文档
- [x] 10. 自我审查：三层分层、字段白名单、三段式日志、全 POST、新增代码无 Mock、无跨端修改
- [x] 11. 测试验证：PHP 语法、autoload、路由、详情查询、保存回滚测试及非法参数

## 已完成

- [x] 删除 PHP 数据库配置中所有 Socket 相关项，恢复原生 TCP 连接配置
- [x] 用 PHP 8.2 通过 `127.0.0.1:3306` 成功连接 `survey_db`
- [x] 新增 `POST /admin/style/detail` 样式配置详情接口
- [x] 新增 `POST /admin/style/save` 样式配置 upsert 接口及同事务审计日志
- [x] 生成样式配置需求方案与项目统一 HTML 接口文档

## 测试结果

- 命令：`/Applications/MAMP/bin/php/php8.2.0/bin/php -l php/config/database.php`
- 输出：`No syntax errors detected in php/config/database.php`
- 命令：Socket 引用全局检查
- 输出：`socket_reference_check=zero_hits`
- 命令：PHP 8.2 PDO 通过 TCP 执行 `SELECT DATABASE(), VERSION()`
- 输出：`tcp_connection=success` / `database_match=yes` / `server_version=8.0.45`
- 命令：`php -l` 检查 5 个新增 PHP 文件及 `route/app.php`
- 输出：全部 `No syntax errors detected`
- 命令：`composer dump-autoload`
- 输出：`Generated autoload files` / `Succeed!`（Composer 本身有 PHP 8.2 deprecation 提示，不影响生成）
- 命令：`php think route:list`
- 输出：`admin/style/detail` 和 `admin/style/save` 均为 POST 并正确指向 `Style/*Op`
- 命令：真实库调用 `StyleService::getDetail(1)`
- 输出：返回现有 `#FE8F22` / `#FFFFFF` / Banner URL
- 命令：外层事务内调用 `StyleService::save()` 并回滚
- 输出：`audit_increment_inside=1` / `rollback_payload_restored=true` / `rollback_audit_restored=true`
- 命令：非法主题色、非法 Banner URL、不存在商户验证
- 输出：分别拦截为 `theme_color 必须是 #RRGGBB 格式`、`banner_url 必须是完整的 HTTP/HTTPS URL`、`商户不存在`
- 命令：本地 HTTP curl 请求两个新路由（未签名）
- 输出：两个路由均返回 `{"code":400,"msg":"app_id 不能为空","data":[]}`，证明路由与 `AdminSign` 中间件生效
- 命令：HTML DOM 解析 + JavaScript `new Function` 语法检查
- 输出：`html_parse=ok` / `javascript_syntax=ok` / `api_count=2`
- 说明：浏览器安全策略拒绝直接打开本地 `file://` 文档，未进行规避；已用静态解析和 JS 语法检查替代。
