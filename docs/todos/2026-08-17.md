# 2026-08-17 任务清单

> 状态：已完成

## 待办

- [x] 1. 删除 `php/config/database.php` 中非原生的 `socket` 连接配置
- [x] 2. 删除 `php/.env` 和 `php/.env.example` 中多余的 `socket=` 配置项
- [x] 3. 影响面检查：确认 PHP 代码内无 `database.socket` / `socket` 数据库配置引用
- [x] 4. 自我审查：确认仅改动 PHP 端上述 3 个配置文件，不改 Vue
- [x] 5. 测试验证：检查 INI 可解析，并通过 `127.0.0.1:3306` 执行只读连通性测试

## 已完成

- [x] 删除 PHP 数据库配置中所有 Socket 相关项，恢复原生 TCP 连接配置
- [x] 用 PHP 8.2 通过 `127.0.0.1:3306` 成功连接 `survey_db`

## 测试结果

- 命令：`/Applications/MAMP/bin/php/php8.2.0/bin/php -l php/config/database.php`
- 输出：`No syntax errors detected in php/config/database.php`
- 命令：Socket 引用全局检查
- 输出：`socket_reference_check=zero_hits`
- 命令：PHP 8.2 PDO 通过 TCP 执行 `SELECT DATABASE(), VERSION()`
- 输出：`tcp_connection=success` / `database_match=yes` / `server_version=8.0.45`
